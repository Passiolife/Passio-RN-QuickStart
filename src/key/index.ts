import { PASSIO_KEY as passioKey } from '@env';

export const PASSIO_KEY = passioKey

// If it's showing error, then please create `key` folder at root of the example folder
// and create folder name with `key` and create index.ts file and add your passio key in that file.
// We don't use env to avoid the setup of env variables in the machine.
